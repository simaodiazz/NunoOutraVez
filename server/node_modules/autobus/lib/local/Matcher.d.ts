interface Data<T = undefined> {
    criteria: string | RegExp;
    data: T;
}
interface Matcher {
    add<T>(criteria: string, data?: T): void;
    match<T = any>(criteria: string): T[];
    remove<T>(criteria: string, data?: T): void;
    isWildcardChannel(str: string): boolean;
}
declare class Matcher {
    _matchers: Data<any>[];
    constructor();
    private makeRegEx;
    private pregQuote;
}
export default Matcher;
